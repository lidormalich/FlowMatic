{
  "meta": {
    "name": "Hebrew Calendar Component System",
    "description": "מערכת לוח שנה עברי מלאה עם תצוגת גריד, תצוגת רשימה, חגים עבריים, פרשות השבוע, וזמני שבת. משתמשת ב-Hebcal API לנתונים עבריים.",
    "language": "he-IL",
    "direction": "RTL",
    "framework": "React",
    "styling": "Tailwind CSS",
    "externalAPIs": [
      {
        "name": "Hebcal",
        "baseURL": "https://www.hebcal.com/hebcal",
        "purpose": "לקבלת חגים עבריים, פרשות השבוע, זמני הדלקת נרות וזמני הבדלה",
        "location": "Tel Aviv (geonameid: 281184)"
      }
    ]
  },
  "architecture": {
    "overview": "המערכת בנויה מ-3 שכבות: שירותים (services) → הוק (hook) → קומפוננטות (components). הנתונים מ-Hebcal API עוברים דרך שירות ה-hebcal, מתמזגים עם נתוני תורים ב-hook, ומוצגים בקומפוננטות.",
    "dataFlow": "hebcal.js (API) → useCalendarData.js (hook - merges data) → CalendarView.js / ListView.js (display)",
    "calendarDataStructure": {
      "description": "הנתונים מאורגנים לפי תאריך (YYYY-MM-DD) כ-object עם מפתחות תאריכים",
      "example": {
        "2026-02-14": {
          "appointments": [
            {
              "_id": "...",
              "name": "שם לקוח",
              "address": "כתובת",
              "phone": "050-1234567",
              "timeRange": "10:00-12:00",
              "jobNature": "תיאור",
              "date": "2026-02-14T00:00:00.000Z"
            }
          ],
          "holidays": [
            {
              "text": "ט״ו בשבט (Hebrew text from API)",
              "category": "holiday"
            }
          ],
          "shabbat": {
            "text": "הדלקת נרות",
            "time": "16:52"
          },
          "parasha": {
            "text": "פרשת בשלח",
            "category": "parashat"
          }
        }
      }
    },
    "hebcalEventCategories": {
      "holiday": "חגים - מוצגים באופן בולט בצבע כתום",
      "parashat": "פרשת השבוע - מוצגת בצבע סגול עם אייקון 📖",
      "candles": "הדלקת נרות שבת - מוצגת בצבע אפור עם אייקון 🕯️",
      "havdalah": "הבדלה - מוצגת יחד עם זמני שבת"
    },
    "criticalNotes": [
      "חשוב מאוד: להשתמש ב-event.hebrew (לא event.title) מהAPI של Hebcal כדי לקבל טקסט בעברית",
      "החודשים ב-Hebcal הם 1-indexed (ינואר=1) בעוד ב-JavaScript הם 0-indexed (ינואר=0)",
      "המיקום מוגדר לתל אביב - ניתן לשנות את geonameid למיקום אחר",
      "יש מנגנון cache של 24 שעות כדי לא להעמיס על ה-API"
    ]
  },
  "files": {
    "services/hebcal.js": {
      "description": "שירות לקריאת נתונים מ-Hebcal API. כולל cache in-memory של 24 שעות.",
      "exports": ["fetchHebcalData", "getCachedMonth", "setCachedMonth"],
      "dependencies": [],
      "code": "const HEBCAL_BASE = 'https://www.hebcal.com/hebcal';\nconst HEBCAL_PARAMS = {\n  v: 1,\n  cfg: 'json',\n  maj: 'on',\n  min: 'on',\n  mod: 'on',\n  nx: 'on',\n  s: 'on',\n  i: 'on',\n  lg: 'h',\n  geo: 'geoname',\n  geonameid: 281184\n};\n\nexport const fetchHebcalData = async (year, month) => {\n  const params = new URLSearchParams({\n    ...HEBCAL_PARAMS,\n    year,\n    month: month + 1 // Hebcal uses 1-indexed months\n  });\n\n  const response = await fetch(`${HEBCAL_BASE}?${params}`);\n\n  if (!response.ok) {\n    throw new Error('Failed to fetch Hebcal data');\n  }\n\n  const result = await response.json();\n\n  // CRITICAL: Extract event.hebrew for Hebrew text, NOT event.title\n  return result.items.map(item => ({\n    date: item.date,\n    hebrew: item.hebrew,        // ← USE THIS FOR DISPLAY (Hebrew text)\n    title: item.title,          // English (backup only)\n    category: item.category,\n    time: item.title.match(/\\d{2}:\\d{2}/)?.[0] // Extract time if present\n  }));\n};\n\n// Simple in-memory cache with 24-hour expiry\nconst cache = new Map();\nconst CACHE_DURATION = 1000 * 60 * 60 * 24; // 24 hours\n\nexport const getCachedMonth = (key) => {\n  const cached = cache.get(key);\n  if (cached && Date.now() - cached.timestamp < CACHE_DURATION) {\n    return cached.data;\n  }\n  cache.delete(key);\n  return null;\n};\n\nexport const setCachedMonth = (key, data) => {\n  cache.set(key, { data, timestamp: Date.now() });\n};"
    },
    "services/api.js": {
      "description": "שירות API לתורים (appointments). כולל CRUD מלא עם JWT authentication. צריך להתאים את ה-endpoints לפרויקט החדש.",
      "exports": ["fetchAppointments", "createAppointment", "updateAppointment", "deleteAppointment", "fetchTrash", "restoreAppointment"],
      "dependencies": [],
      "adaptationNotes": "צריך להתאים את API_BASE ואת ה-endpoints (/api/jobs) למבנה של הפרויקט החדש. ה-authentication מבוסס על JWT token ב-localStorage.",
      "code": "const API_BASE = '/api';\n\n// Get token from localStorage\nconst getToken = () => {\n  return localStorage.getItem('token');\n};\n\n// Get headers with authentication\nconst getAuthHeaders = () => {\n  const token = getToken();\n  return {\n    'Content-Type': 'application/json',\n    ...(token && { 'Authorization': `Bearer ${token}` })\n  };\n};\n\nexport const fetchAppointments = async (startDate, endDate) => {\n  const params = new URLSearchParams();\n  if (startDate) params.append('startDate', startDate);\n  if (endDate) params.append('endDate', endDate);\n\n  const response = await fetch(`${API_BASE}/jobs?${params}`, {\n    headers: getAuthHeaders()\n  });\n\n  if (!response.ok) {\n    throw new Error('Failed to fetch appointments');\n  }\n\n  const result = await response.json();\n  return result.data;\n};\n\nexport const createAppointment = async (appointmentData) => {\n  const response = await fetch(`${API_BASE}/jobs`, {\n    method: 'POST',\n    headers: getAuthHeaders(),\n    body: JSON.stringify(appointmentData)\n  });\n\n  const result = await response.json();\n\n  if (!response.ok) {\n    throw new Error(result.message || 'Failed to create appointment');\n  }\n\n  return result.data;\n};\n\nexport const updateAppointment = async (id, appointmentData) => {\n  const response = await fetch(`${API_BASE}/jobs/${id}`, {\n    method: 'PUT',\n    headers: getAuthHeaders(),\n    body: JSON.stringify(appointmentData)\n  });\n\n  const result = await response.json();\n\n  if (!response.ok) {\n    throw new Error(result.message || 'Failed to update appointment');\n  }\n\n  return result.data;\n};\n\nexport const deleteAppointment = async (id) => {\n  const response = await fetch(`${API_BASE}/jobs/${id}`, {\n    method: 'DELETE',\n    headers: getAuthHeaders()\n  });\n\n  const result = await response.json();\n\n  if (!response.ok) {\n    throw new Error(result.message || 'Failed to delete appointment');\n  }\n\n  return result.data;\n};\n\nexport const fetchTrash = async () => {\n  const response = await fetch(`${API_BASE}/jobs/trash`, {\n    headers: getAuthHeaders()\n  });\n\n  if (!response.ok) {\n    throw new Error('Failed to fetch trash');\n  }\n\n  const result = await response.json();\n  return result.data;\n};\n\nexport const restoreAppointment = async (id) => {\n  const response = await fetch(`${API_BASE}/jobs/${id}/restore`, {\n    method: 'POST',\n    headers: getAuthHeaders()\n  });\n\n  const result = await response.json();\n\n  if (!response.ok) {\n    throw new Error(result.message || 'Failed to restore appointment');\n  }\n\n  return result.data;\n};"
    },
    "hooks/useCalendarData.js": {
      "description": "הוק (Custom Hook) שממזג נתוני תורים מה-backend עם נתוני לוח שנה עברי מ-Hebcal. מארגן את הכל לפי תאריך.",
      "exports": ["useCalendarData (default)"],
      "dependencies": ["../services/api (fetchAppointments)", "../services/hebcal (fetchHebcalData, getCachedMonth, setCachedMonth)"],
      "params": {
        "year": "number - שנה (למשל 2026)",
        "month": "number - חודש 0-indexed (0=ינואר, 11=דצמבר)",
        "refreshTrigger": "number - מספר שמשתנה כשצריך לרענן את הנתונים"
      },
      "returns": {
        "calendarData": "object - מפתחות YYYY-MM-DD עם appointments, holidays, shabbat, parasha",
        "loading": "boolean",
        "error": "string | null"
      },
      "code": "import { useState, useEffect, useMemo } from 'react';\nimport { fetchAppointments } from '../services/api';\nimport { fetchHebcalData, getCachedMonth, setCachedMonth } from '../services/hebcal';\n\nconst useCalendarData = (year, month, refreshTrigger = 0) => {\n  const [appointments, setAppointments] = useState([]);\n  const [hebcalEvents, setHebcalEvents] = useState([]);\n  const [loading, setLoading] = useState(true);\n  const [error, setError] = useState(null);\n\n  // Fetch data on mount, when month changes, or when refreshTrigger changes\n  useEffect(() => {\n    const fetchData = async () => {\n      setLoading(true);\n      setError(null);\n\n      try {\n        // Calculate date range for the month\n        const startDate = new Date(year, month, 1).toISOString();\n        const endDate = new Date(year, month + 1, 0, 23, 59, 59).toISOString();\n\n        // Fetch appointments from backend\n        const appointmentsData = await fetchAppointments(startDate, endDate);\n\n        // Fetch Hebcal data (check cache first)\n        const cacheKey = `${year}-${month}`;\n        let hebcalData = getCachedMonth(cacheKey);\n\n        if (!hebcalData) {\n          hebcalData = await fetchHebcalData(year, month);\n          setCachedMonth(cacheKey, hebcalData);\n        }\n\n        setAppointments(appointmentsData);\n        setHebcalEvents(hebcalData);\n      } catch (err) {\n        setError(err.message);\n        console.error('Error fetching calendar data:', err);\n      } finally {\n        setLoading(false);\n      }\n    };\n\n    fetchData();\n  }, [year, month, refreshTrigger]);\n\n  // Merge and group data by date (YYYY-MM-DD)\n  const calendarData = useMemo(() => {\n    const grouped = {};\n\n    // Group appointments by date\n    appointments.forEach(apt => {\n      const dateKey = new Date(apt.date).toISOString().split('T')[0];\n      if (!grouped[dateKey]) {\n        grouped[dateKey] = { appointments: [], holidays: [], shabbat: null, parasha: null };\n      }\n      grouped[dateKey].appointments.push(apt);\n    });\n\n    // Group Hebcal events by date\n    hebcalEvents.forEach(event => {\n      const dateKey = event.date.split('T')[0]; // ISO format\n\n      if (!grouped[dateKey]) {\n        grouped[dateKey] = { appointments: [], holidays: [], shabbat: null, parasha: null };\n      }\n\n      // CRITICAL: Use event.hebrew for Hebrew text, NOT event.title\n      if (event.category === 'holiday') {\n        grouped[dateKey].holidays.push({\n          text: event.hebrew, // HEBREW TEXT\n          category: event.category\n        });\n      } else if (event.category === 'parashat') {\n        // Weekly Torah portion (Parashat HaShavua)\n        grouped[dateKey].parasha = {\n          text: event.hebrew, // HEBREW TEXT\n          category: event.category\n        };\n      } else if (event.category === 'candles' || event.category === 'havdalah') {\n        // Shabbat candle lighting or Havdalah\n        grouped[dateKey].shabbat = {\n          text: event.hebrew, // HEBREW TEXT\n          time: event.time\n        };\n      }\n    });\n\n    return grouped;\n  }, [appointments, hebcalEvents]);\n\n  return { calendarData, loading, error };\n};\n\nexport default useCalendarData;"
    },
    "components/CalendarView.js": {
      "description": "קומפוננטה ראשית - תצוגת לוח שנה בגריד 7 עמודות (א׳-ש׳). כוללת ניווט בין חודשים, מעבר בין תצוגת לוח לתצוגת רשימה, הצגת תורים/חגים/שבת/פרשה בכל יום, ותצוגת פרטים inline למובייל.",
      "exports": ["CalendarView (default)"],
      "dependencies": [
        "../hooks/useCalendarData",
        "./AppointmentModal",
        "./ListView",
        "../services/auth (isAuthenticated, isAdmin)",
        "../services/api (updateAppointment, deleteAppointment)"
      ],
      "features": [
        "תצוגת גריד חודשית עם ימי שבוע בעברית (א׳-ש׳)",
        "ניווט בין חודשים עם חצים וכפתור 'היום'",
        "מעבר בין תצוגת לוח לתצוגת רשימה (נשמר ב-localStorage)",
        "הצגת שמות תורים בכל יום בצבע כחול",
        "הצגת חגים עבריים בצבע כתום",
        "הצגת פרשת השבוע בצבע סגול עם אייקון 📖",
        "הצגת זמני שבת באפור",
        "תצוגת פרטי יום inline למובייל (מתחת ללוח)",
        "מודאל פרטי יום לדסקטופ",
        "כפתור + צף למובייל להוספת תור חדש",
        "רספונסיבי מלא - מותאם למובייל ודסקטופ"
      ],
      "colorScheme": {
        "primary": "#007AFF (כחול - כפתורים, תורים, הדגשות)",
        "primaryDark": "#0056CC (כחול כהה - hover, gradient)",
        "holidays": "#FF9500 (כתום - חגים)",
        "parasha": "#5856D6 (סגול - פרשת השבוע)",
        "shabbat": "#8E8E93 (אפור - זמני שבת)",
        "success": "green-500 (ירוק - WhatsApp, יצירת תור)",
        "danger": "red (אדום - מחיקה, שגיאות)"
      },
      "code": "import {useState, useEffect} from 'react';\nimport {useNavigate} from 'react-router-dom';\nimport {isAuthenticated, isAdmin} from '../services/auth';\nimport {updateAppointment, deleteAppointment} from '../services/api';\nimport useCalendarData from '../hooks/useCalendarData';\nimport AppointmentModal from './AppointmentModal';\nimport ListView from './ListView';\n\nfunction CalendarView() {\n  const navigate = useNavigate();\n  const [currentDate, setCurrentDate] = useState(new Date());\n  const [selectedDate, setSelectedDate] = useState(null);\n  const [refreshTrigger, setRefreshTrigger] = useState(0);\n\n  // View mode: 'calendar' or 'list' - persist in localStorage\n  const [viewMode, setViewMode] = useState(() => {\n    return localStorage.getItem('viewMode') || 'calendar';\n  });\n\n  const year = currentDate.getFullYear();\n  const month = currentDate.getMonth();\n\n  const authenticated = isAuthenticated();\n  const admin = isAdmin();\n\n  const {calendarData, loading, error} = useCalendarData(\n    year,\n    month,\n    refreshTrigger,\n  );\n\n  // Generate calendar grid\n  const getDaysInMonth = () => {\n    const firstDay = new Date(year, month, 1);\n    const lastDay = new Date(year, month + 1, 0);\n    const daysInMonth = lastDay.getDate();\n    const startingDayOfWeek = firstDay.getDay();\n\n    const days = [];\n\n    // Empty cells for days before month starts\n    for (let i = 0; i < startingDayOfWeek; i++) {\n      days.push(null);\n    }\n\n    // Days of the month\n    for (let day = 1; day <= daysInMonth; day++) {\n      days.push(day);\n    }\n\n    return days;\n  };\n\n  const handlePrevMonth = () => {\n    setCurrentDate(new Date(year, month - 1));\n    setSelectedDate(null);\n  };\n\n  const handleNextMonth = () => {\n    setCurrentDate(new Date(year, month + 1));\n    setSelectedDate(null);\n  };\n\n  const handleGoToToday = () => {\n    setCurrentDate(new Date());\n    setSelectedDate(null);\n  };\n\n  const handleDayClick = (dateKey, dayData) => {\n    setSelectedDate({date: dateKey, ...dayData});\n  };\n\n  const handleCloseModal = () => {\n    setSelectedDate(null);\n  };\n\n  const handleUpdate = () => {\n    setRefreshTrigger(prev => prev + 1);\n  };\n\n  const toggleViewMode = () => {\n    const newMode = viewMode === 'calendar' ? 'list' : 'calendar';\n    setViewMode(newMode);\n    localStorage.setItem('viewMode', newMode);\n  };\n\n  useEffect(() => {\n    localStorage.setItem('viewMode', viewMode);\n  }, [viewMode]);\n\n  const days = getDaysInMonth();\n  const monthName = currentDate.toLocaleDateString('he-IL', {\n    month: 'long',\n    year: 'numeric',\n  });\n\n  if (!authenticated) {\n    return (\n      <div className=\"max-w-4xl mx-auto p-6 mt-20\">\n        <div className=\"bg-white rounded-2xl shadow-sm p-8 text-center\">\n          <div className=\"text-6xl mb-4\">📅</div>\n          <h2 className=\"text-2xl font-bold mb-4\">ברוכים הבאים ליומן תורים</h2>\n          <p className=\"text-gray-600 mb-6\">\n            כדי לראות את לוח השנה ולנהל את התורים, יש להתחבר תחילה.\n          </p>\n          <div className=\"flex gap-4 justify-center\">\n            <button\n              onClick={() => navigate('/login')}\n              className=\"bg-[#007AFF] text-white px-6 py-3 rounded-xl font-medium hover:bg-[#0056CC] active:scale-[0.98] transition-all\">\n              התחבר\n            </button>\n            <button\n              onClick={() => navigate('/register')}\n              className=\"bg-gray-100 text-gray-700 px-6 py-3 rounded-xl font-medium hover:bg-gray-200 active:scale-[0.98] transition-all\">\n              הירשם\n            </button>\n          </div>\n        </div>\n      </div>\n    );\n  }\n\n  if (!admin) {\n    return (\n      <div className=\"max-w-4xl mx-auto p-6 mt-20\">\n        <div className=\"bg-white rounded-2xl shadow-sm p-8 text-center\">\n          <div className=\"text-6xl mb-4\">🔒</div>\n          <h2 className=\"text-2xl font-bold mb-4\">אין גישה</h2>\n          <p className=\"text-gray-600 mb-6\">\n            החשבון שלך נרשם בהצלחה, אך רק מנהלים יכולים לגשת למערכת.\n            <br />\n            צור קשר עם המנהל לקבלת הרשאות.\n          </p>\n        </div>\n      </div>\n    );\n  }\n\n  if (loading) {\n    return (\n      <div className=\"flex justify-center items-center h-96\">\n        <p className=\"text-lg text-gray-500\">טוען...</p>\n      </div>\n    );\n  }\n\n  if (error) {\n    return (\n      <div className=\"max-w-4xl mx-auto p-6\">\n        <div className=\"bg-red-50 border border-red-200 rounded-xl p-4 text-red-700\">\n          שגיאה בטעינת נתונים: {error}\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"max-w-6xl mx-auto p-3 md:p-6 relative pb-22 md:pb-6\">\n      {/* Header with Month Navigator and View Toggle */}\n      <div className=\"bg-white rounded-2xl shadow-sm p-4 md:p-6 mb-4 md:mb-6\">\n        <div className=\"flex justify-between items-center gap-2 md:gap-4 flex-wrap\">\n          {/* Navigation Arrows */}\n          <button\n            onClick={handleNextMonth}\n            className=\"text-[#007AFF] font-medium px-3 md:px-4 py-2 rounded-xl hover:bg-gray-50 active:scale-[0.98] transition-all order-1\">\n            →\n          </button>\n\n          {/* Month Name + Today Button */}\n          <div className=\"flex items-center gap-2 order-3 sm:order-2 flex-shrink-0\">\n            <h2 className=\"text-xl md:text-2xl font-bold\">\n              {monthName}\n            </h2>\n            <button\n              onClick={handleGoToToday}\n              className=\"text-xs bg-gray-100 text-[#007AFF] px-2.5 py-1.5 rounded-lg font-medium hover:bg-gray-200 active:scale-[0.98] transition-all\"\n            >\n              היום\n            </button>\n          </div>\n\n          {/* View Toggle */}\n          <div className=\"flex items-center gap-1 md:gap-2 bg-gray-100 rounded-xl p-1 order-2 sm:order-3\">\n            <button\n              onClick={() => viewMode !== 'calendar' && toggleViewMode()}\n              className={`px-2 md:px-4 py-2 rounded-lg font-medium text-sm transition-all ${\n                viewMode === 'calendar'\n                  ? 'bg-white text-[#007AFF] shadow-sm'\n                  : 'text-gray-600 hover:text-gray-900'\n              }`}>\n              <span className=\"hidden sm:inline\">📅 לוח</span>\n              <span className=\"sm:hidden\">📅</span>\n            </button>\n            <button\n              onClick={() => viewMode !== 'list' && toggleViewMode()}\n              className={`px-2 md:px-4 py-2 rounded-lg font-medium text-sm transition-all ${\n                viewMode === 'list'\n                  ? 'bg-white text-[#007AFF] shadow-sm'\n                  : 'text-gray-600 hover:text-gray-900'\n              }`}>\n              <span className=\"hidden sm:inline\">📋 רשימה</span>\n              <span className=\"sm:hidden\">📋</span>\n            </button>\n          </div>\n\n          <button\n            onClick={handlePrevMonth}\n            className=\"text-[#007AFF] font-medium px-3 md:px-4 py-2 rounded-xl hover:bg-gray-50 active:scale-[0.98] transition-all order-4\">\n            ←\n          </button>\n        </div>\n      </div>\n\n      {/* Content - Calendar or List View */}\n      {viewMode === 'calendar' ? (\n        <>\n          {/* Calendar Grid */}\n          <div className=\"bg-white rounded-2xl shadow-sm p-2 md:p-6\">\n            {/* Day Headers */}\n            <div className=\"grid grid-cols-7 gap-1 md:gap-2 mb-2 md:mb-4\">\n              {['א׳', 'ב׳', 'ג׳', 'ד׳', 'ה׳', 'ו׳', 'ש׳'].map(day => (\n                <div\n                  key={day}\n                  className=\"text-center font-semibold text-gray-500 py-1 md:py-2 text-xs md:text-sm\">\n                  {day}\n                </div>\n              ))}\n            </div>\n\n            {/* Calendar Days */}\n            <div className=\"grid grid-cols-7 gap-1 md:gap-2\">\n              {days.map((day, index) => {\n                if (!day) {\n                  return <div key={`empty-${index}`} className=\"aspect-square\" />;\n                }\n\n                const dateKey = `${year}-${String(month + 1).padStart(2, '0')}-${String(\n                  day,\n                ).padStart(2, '0')}`;\n                const dayData = calendarData[dateKey] || {\n                  appointments: [],\n                  holidays: [],\n                  shabbat: null,\n                  parasha: null,\n                };\n\n                const hasData =\n                  dayData.appointments.length > 0 ||\n                  dayData.holidays.length > 0 ||\n                  dayData.shabbat ||\n                  dayData.parasha;\n\n                const isSelected = selectedDate && selectedDate.date === dateKey;\n\n                return (\n                  <div\n                    key={day}\n                    onClick={() => handleDayClick(dateKey, dayData)}\n                    className={`aspect-square border rounded-lg md:rounded-xl p-1 md:p-2 transition-all ${\n                      isSelected\n                        ? 'border-[#007AFF] bg-blue-50 ring-2 ring-[#007AFF]/30'\n                        : hasData\n                        ? 'border-[#007AFF]/30 bg-blue-50/30'\n                        : 'border-gray-200 hover:bg-gray-50'\n                    } cursor-pointer hover:shadow-md hover:scale-105`}>\n                    <div className=\"text-xs md:text-sm font-semibold mb-0.5 md:mb-1\">\n                      {day}\n                    </div>\n\n                    {/* Appointments - Show all names */}\n                    {dayData.appointments.length > 0 && (\n                      <div className=\"space-y-0.5\">\n                        {dayData.appointments.map((apt, idx) => (\n                          <div\n                            key={idx}\n                            className=\"text-[10px] md:text-xs bg-[#007AFF] text-white rounded px-1 md:px-1.5 py-0.5 truncate font-medium\"\n                            title={apt.name}>\n                            {apt.name}\n                          </div>\n                        ))}\n                      </div>\n                    )}\n\n                    {/* Parasha (Torah Portion) - Show on all sizes */}\n                    {dayData.parasha && (\n                      <div\n                        className=\"text-[9px] md:text-xs text-[#5856D6] font-semibold truncate\"\n                        title={dayData.parasha.text}>\n                        📖 {dayData.parasha.text}\n                      </div>\n                    )}\n\n                    {/* Holidays - Show text on all sizes */}\n                    {dayData.holidays.map((holiday, idx) => (\n                      <div\n                        key={idx}\n                        className=\"text-[9px] md:text-xs text-[#FF9500] font-medium truncate\"\n                        title={holiday.text}>\n                        {holiday.text}\n                      </div>\n                    ))}\n\n                    {/* Shabbat - Show text on all sizes */}\n                    {dayData.shabbat && (\n                      <div\n                        className=\"text-[9px] md:text-xs text-[#8E8E93] truncate\"\n                        title={dayData.shabbat.text}>\n                        {dayData.shabbat.text}\n                      </div>\n                    )}\n                  </div>\n                );\n              })}\n            </div>\n          </div>\n\n          {/* Inline Day Details (Mobile) - shown below calendar */}\n          {selectedDate && (\n            <div className=\"md:hidden mt-4\">\n              <InlineDayDetails\n                date={selectedDate.date}\n                appointments={selectedDate.appointments}\n                holidays={selectedDate.holidays}\n                shabbat={selectedDate.shabbat}\n                parasha={selectedDate.parasha}\n                onUpdate={handleUpdate}\n                onClose={handleCloseModal}\n              />\n            </div>\n          )}\n\n          {/* Appointment Modal (Desktop only) */}\n          {selectedDate && (\n            <div className=\"hidden md:block\">\n              <AppointmentModal\n                date={selectedDate.date}\n                appointments={selectedDate.appointments}\n                holidays={selectedDate.holidays}\n                shabbat={selectedDate.shabbat}\n                onClose={handleCloseModal}\n                onUpdate={handleUpdate}\n              />\n            </div>\n          )}\n        </>\n      ) : (\n        /* List View */\n        <ListView\n          calendarData={calendarData}\n          year={year}\n          month={month}\n          onUpdate={handleUpdate}\n        />\n      )}\n\n      {/* Floating + Button (Mobile) */}\n      <button\n        onClick={() => {\n          if (selectedDate) {\n            // If a date is selected, open create in that date's context\n            handleDayClick(selectedDate.date, selectedDate);\n          } else {\n            navigate('/new');\n          }\n        }}\n        className=\"md:hidden fixed bottom-6 left-6 w-14 h-14 bg-[#007AFF] text-white rounded-full shadow-lg flex items-center justify-center text-3xl font-light active:scale-[0.95] transition-transform z-40 hover:bg-[#0056CC]\"\n      >\n        +\n      </button>\n    </div>\n  );\n}\n\n// Inline day details component for mobile\nfunction InlineDayDetails({ date, appointments, holidays, shabbat, parasha, onUpdate, onClose }) {\n  const formattedDate = new Date(date).toLocaleDateString('he-IL', {\n    weekday: 'long',\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric'\n  });\n\n  return (\n    <div className=\"bg-white rounded-2xl shadow-sm overflow-hidden\">\n      {/* Header */}\n      <div className=\"bg-gradient-to-r from-[#007AFF] to-[#0056CC] text-white p-4 flex justify-between items-center\">\n        <div>\n          <h3 className=\"text-lg font-bold\">{formattedDate}</h3>\n          <p className=\"text-sm opacity-90\">\n            {appointments.length > 0\n              ? `${appointments.length} תור${appointments.length > 1 ? 'ים' : ''}`\n              : 'אין תורים'}\n          </p>\n        </div>\n        <button\n          onClick={onClose}\n          className=\"text-white/80 hover:text-white text-2xl leading-none\"\n        >\n          ×\n        </button>\n      </div>\n\n      <div className=\"p-4 space-y-3\">\n        {/* Holidays & Shabbat */}\n        {(holidays.length > 0 || shabbat || parasha) && (\n          <div className=\"bg-gradient-to-br from-orange-50 to-amber-50 rounded-xl p-3 space-y-1.5\">\n            {parasha && (\n              <div className=\"flex items-center gap-2 text-sm\">\n                <span>📖</span>\n                <span className=\"text-purple-700 font-semibold\">{parasha.text}</span>\n              </div>\n            )}\n            {holidays.map((holiday, idx) => (\n              <div key={idx} className=\"flex items-center gap-2 text-sm\">\n                <span>🕎</span>\n                <span className=\"text-orange-900 font-semibold\">{holiday.text}</span>\n              </div>\n            ))}\n            {shabbat && (\n              <div className=\"flex items-center gap-2 text-sm\">\n                <span>🕯️</span>\n                <span className=\"text-gray-700\">{shabbat.text}</span>\n                {shabbat.time && (\n                  <span className=\"text-gray-500\">({shabbat.time})</span>\n                )}\n              </div>\n            )}\n          </div>\n        )}\n\n        {/* Appointments List */}\n        {appointments.length > 0 ? (\n          <div className=\"space-y-2\">\n            {appointments.map((apt) => (\n              <AppointmentCard key={apt._id} appointment={apt} onUpdate={onUpdate} />\n            ))}\n          </div>\n        ) : (\n          <p className=\"text-center text-gray-400 py-4\">אין תורים ביום זה</p>\n        )}\n      </div>\n    </div>\n  );\n}\n\n// Appointment card used in inline view\nfunction AppointmentCard({ appointment, onUpdate }) {\n  const [editing, setEditing] = useState(false);\n  const [editForm, setEditForm] = useState({});\n  const [loading, setLoading] = useState(false);\n\n  const formatPhone = (phone) => {\n    return phone.replace(/^0/, '972');\n  };\n\n  const handleEdit = () => {\n    setEditing(true);\n    setEditForm({\n      name: appointment.name,\n      address: appointment.address,\n      phone: appointment.phone || '',\n      timeRange: appointment.timeRange || '',\n      jobNature: appointment.jobNature || '',\n    });\n  };\n\n  const handleSave = async () => {\n    setLoading(true);\n    try {\n      await updateAppointment(appointment._id, editForm);\n      setEditing(false);\n      onUpdate();\n    } catch {\n      // silently fail\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const handleDelete = async () => {\n    if (!window.confirm(`העבר את התור של ${appointment.name} לסל מחזור?`)) return;\n    setLoading(true);\n    try {\n      await deleteAppointment(appointment._id);\n      onUpdate();\n    } catch {\n      // silently fail\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  if (editing) {\n    const inputClass = \"w-full px-3 py-2 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-[#007AFF] focus:border-transparent\";\n    return (\n      <div className=\"bg-gray-50 rounded-xl p-3 space-y-2\">\n        <input type=\"text\" value={editForm.name} onChange={e => setEditForm({...editForm, name: e.target.value})} placeholder=\"שם\" className={inputClass} />\n        <input type=\"text\" value={editForm.address} onChange={e => setEditForm({...editForm, address: e.target.value})} placeholder=\"כתובת\" className={inputClass} />\n        <input type=\"tel\" value={editForm.phone} onChange={e => setEditForm({...editForm, phone: e.target.value})} placeholder=\"טלפון\" className={inputClass} />\n        <input type=\"text\" value={editForm.timeRange} onChange={e => setEditForm({...editForm, timeRange: e.target.value})} placeholder=\"שעות\" className={inputClass} />\n        <textarea value={editForm.jobNature} onChange={e => setEditForm({...editForm, jobNature: e.target.value})} placeholder=\"מהות העבודה\" rows=\"2\" className={`${inputClass} resize-none`} />\n        <div className=\"flex gap-2\">\n          <button onClick={handleSave} disabled={loading} className=\"flex-1 bg-[#007AFF] text-white py-2 rounded-lg text-sm font-medium disabled:opacity-50\">\n            {loading ? 'שומר...' : 'שמור'}\n          </button>\n          <button onClick={() => setEditing(false)} className=\"flex-1 bg-gray-200 text-gray-700 py-2 rounded-lg text-sm font-medium\">\n            ביטול\n          </button>\n        </div>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"bg-gray-50 rounded-xl p-3\">\n      <div className=\"flex items-start justify-between gap-2\">\n        <div className=\"flex-1 space-y-1\">\n          <div className=\"font-semibold text-gray-900\">{appointment.name}</div>\n          <div className=\"text-sm text-gray-600 flex items-center gap-1\">\n            <span>📍</span> {appointment.address}\n          </div>\n          {appointment.phone && (\n            <div className=\"text-sm text-gray-600 flex items-center gap-1\">\n              <span>📞</span>\n              <a href={`tel:${appointment.phone}`} className=\"text-[#007AFF]\">{appointment.phone}</a>\n            </div>\n          )}\n          {appointment.timeRange && (\n            <div className=\"text-sm text-gray-600 flex items-center gap-1\">\n              <span>🕐</span> {appointment.timeRange}\n            </div>\n          )}\n          {appointment.jobNature && (\n            <div className=\"text-sm text-gray-500\">{appointment.jobNature}</div>\n          )}\n\n          {/* WhatsApp & Waze buttons */}\n          <div className=\"flex gap-2 mt-2\">\n            {appointment.phone && (\n              <a\n                href={`https://wa.me/${formatPhone(appointment.phone)}`}\n                target=\"_blank\"\n                rel=\"noopener noreferrer\"\n                className=\"inline-flex items-center gap-1 bg-green-500 text-white px-3 py-1.5 rounded-lg text-xs font-medium hover:bg-green-600 active:scale-[0.98] transition-all\"\n                onClick={e => e.stopPropagation()}\n              >\n                💬 WhatsApp\n              </a>\n            )}\n            {appointment.address && (\n              <a\n                href={`https://waze.com/ul?q=${encodeURIComponent(appointment.address)}`}\n                target=\"_blank\"\n                rel=\"noopener noreferrer\"\n                className=\"inline-flex items-center gap-1 bg-[#33ccff] text-white px-3 py-1.5 rounded-lg text-xs font-medium hover:bg-[#00b8f0] active:scale-[0.98] transition-all\"\n                onClick={e => e.stopPropagation()}\n              >\n                🗺️ Waze\n              </a>\n            )}\n          </div>\n        </div>\n      </div>\n\n      {/* Edit/Delete buttons */}\n      <div className=\"flex gap-2 mt-3 pt-2 border-t border-gray-200\">\n        <button\n          onClick={handleEdit}\n          disabled={loading}\n          className=\"flex-1 bg-[#007AFF] text-white py-2 rounded-lg text-xs font-medium active:scale-[0.98] transition-all disabled:opacity-50\"\n        >\n          ✏️ ערוך\n        </button>\n        <button\n          onClick={handleDelete}\n          disabled={loading}\n          className=\"flex-1 bg-red-50 text-red-600 py-2 rounded-lg text-xs font-medium active:scale-[0.98] transition-all disabled:opacity-50\"\n        >\n          🗑️ סל מחזור\n        </button>\n      </div>\n    </div>\n  );\n}\n\nexport default CalendarView;"
    },
    "components/ListView.js": {
      "description": "תצוגת רשימה - מציגה תורים ממוינים לפי תאריך עם header צבעוני לכל יום, חגים, שבת ופרשה.",
      "exports": ["ListView (default)"],
      "dependencies": ["./AppointmentModal"],
      "code": "import { useState } from 'react';\nimport AppointmentModal from './AppointmentModal';\n\nfunction ListView({ calendarData, year, month, onUpdate }) {\n  const [selectedDate, setSelectedDate] = useState(null);\n\n  // Get all dates with appointments sorted\n  const datesWithAppointments = Object.keys(calendarData)\n    .filter(dateKey => {\n      const date = new Date(dateKey);\n      return date.getFullYear() === year &&\n             date.getMonth() === month &&\n             calendarData[dateKey].appointments.length > 0;\n    })\n    .sort((a, b) => new Date(a) - new Date(b));\n\n  const handleAppointmentClick = (dateKey) => {\n    const dayData = calendarData[dateKey];\n    setSelectedDate({ date: dateKey, ...dayData });\n  };\n\n  const handleCloseModal = () => {\n    setSelectedDate(null);\n  };\n\n  // Format date for display\n  const formatDate = (dateKey) => {\n    return new Date(dateKey).toLocaleDateString('he-IL', {\n      weekday: 'long',\n      year: 'numeric',\n      month: 'long',\n      day: 'numeric'\n    });\n  };\n\n  // Get day of month\n  const getDayOfMonth = (dateKey) => {\n    return new Date(dateKey).getDate();\n  };\n\n  if (datesWithAppointments.length === 0) {\n    return (\n      <div className=\"bg-white rounded-2xl shadow-sm p-12 text-center\">\n        <div className=\"text-6xl mb-4\">📅</div>\n        <h3 className=\"text-xl font-semibold text-gray-900 mb-2\">\n          אין תורים החודש\n        </h3>\n        <p className=\"text-gray-500\">\n          לחץ על \"תור חדש\" כדי להוסיף תור\n        </p>\n      </div>\n    );\n  }\n\n  return (\n    <div className=\"space-y-3\">\n      {datesWithAppointments.map((dateKey) => {\n        const dayData = calendarData[dateKey];\n        const hasHolidaysOrShabbat = dayData.holidays.length > 0 || dayData.shabbat || dayData.parasha;\n\n        return (\n          <div\n            key={dateKey}\n            className=\"bg-white rounded-2xl shadow-sm overflow-hidden hover:shadow-md transition-shadow\"\n          >\n            {/* Date Header */}\n            <div className=\"bg-gradient-to-r from-[#007AFF] to-[#0056CC] text-white px-6 py-4 flex items-center gap-4\">\n              <div className=\"text-center min-w-[60px]\">\n                <div className=\"text-3xl font-bold\">{getDayOfMonth(dateKey)}</div>\n                <div className=\"text-sm opacity-90\">\n                  {new Date(dateKey).toLocaleDateString('he-IL', { month: 'short' })}\n                </div>\n              </div>\n              <div className=\"flex-1\">\n                <div className=\"text-lg font-semibold\">{formatDate(dateKey)}</div>\n                <div className=\"text-sm opacity-90\">\n                  {dayData.appointments.length} תור{dayData.appointments.length > 1 ? 'ים' : ''}\n                </div>\n              </div>\n            </div>\n\n            {/* Holidays & Shabbat */}\n            {hasHolidaysOrShabbat && (\n              <div className=\"bg-gradient-to-br from-orange-50 to-amber-50 px-6 py-3 border-b border-orange-100 space-y-1\">\n                {dayData.parasha && (\n                  <div className=\"flex items-center gap-2 text-sm\">\n                    <span>📖</span>\n                    <span className=\"text-purple-700 font-semibold\">{dayData.parasha.text}</span>\n                  </div>\n                )}\n                {dayData.holidays.map((holiday, idx) => (\n                  <div key={idx} className=\"flex items-center gap-2 text-sm\">\n                    <span>🕎</span>\n                    <span className=\"text-orange-900 font-semibold\">{holiday.text}</span>\n                  </div>\n                ))}\n                {dayData.shabbat && (\n                  <div className=\"flex items-center gap-2 text-sm\">\n                    <span>🕯️</span>\n                    <span className=\"text-gray-700\">{dayData.shabbat.text}</span>\n                    {dayData.shabbat.time && (\n                      <span className=\"text-gray-500\">({dayData.shabbat.time})</span>\n                    )}\n                  </div>\n                )}\n              </div>\n            )}\n\n            {/* Appointments */}\n            <div className=\"p-4 space-y-3\">\n              {dayData.appointments.map((appointment, idx) => (\n                <div\n                  key={appointment._id}\n                  onClick={() => handleAppointmentClick(dateKey)}\n                  className=\"bg-gray-50 rounded-xl p-4 hover:bg-gray-100 cursor-pointer transition-all hover:shadow-sm active:scale-[0.99] group\"\n                >\n                  <div className=\"flex items-start justify-between gap-4\">\n                    <div className=\"flex-1 space-y-2\">\n                      {/* Name & Time */}\n                      <div className=\"flex items-center gap-2 flex-wrap\">\n                        <span className=\"text-lg font-semibold text-gray-900\">\n                          {appointment.name}\n                        </span>\n                        {appointment.timeRange && (\n                          <span className=\"text-sm bg-blue-100 text-blue-700 px-2 py-1 rounded-lg font-medium\">\n                            🕐 {appointment.timeRange}\n                          </span>\n                        )}\n                      </div>\n\n                      {/* Address */}\n                      <div className=\"flex items-start gap-2 text-sm text-gray-600\">\n                        <span className=\"text-base\">📍</span>\n                        <span>{appointment.address}</span>\n                      </div>\n\n                      {/* Phone */}\n                      {appointment.phone && (\n                        <div className=\"flex items-center gap-2 text-sm\">\n                          <span className=\"text-base\">📞</span>\n                          <a\n                            href={`tel:${appointment.phone}`}\n                            onClick={(e) => e.stopPropagation()}\n                            className=\"text-[#007AFF] hover:underline\"\n                          >\n                            {appointment.phone}\n                          </a>\n                        </div>\n                      )}\n\n                      {/* Job Nature (Preview) */}\n                      {appointment.jobNature && (\n                        <div className=\"flex items-start gap-2 text-sm text-gray-600\">\n                          <span className=\"text-base\">📝</span>\n                          <span className=\"line-clamp-2\">{appointment.jobNature}</span>\n                        </div>\n                      )}\n\n                      {/* WhatsApp & Waze */}\n                      <div className=\"flex gap-2 flex-wrap mt-1\">\n                        {appointment.phone && (\n                          <a\n                            href={`https://wa.me/${appointment.phone.replace(/^0/, '972')}`}\n                            target=\"_blank\"\n                            rel=\"noopener noreferrer\"\n                            onClick={(e) => e.stopPropagation()}\n                            className=\"inline-flex items-center gap-1 bg-green-500 text-white px-3 py-1.5 rounded-lg text-xs font-medium hover:bg-green-600 active:scale-[0.98] transition-all\"\n                          >\n                            💬 WhatsApp\n                          </a>\n                        )}\n                        {appointment.address && (\n                          <a\n                            href={`https://waze.com/ul?q=${encodeURIComponent(appointment.address)}`}\n                            target=\"_blank\"\n                            rel=\"noopener noreferrer\"\n                            onClick={(e) => e.stopPropagation()}\n                            className=\"inline-flex items-center gap-1 bg-[#33ccff] text-white px-3 py-1.5 rounded-lg text-xs font-medium hover:bg-[#00b8f0] active:scale-[0.98] transition-all\"\n                          >\n                            🗺️ Waze\n                          </a>\n                        )}\n                      </div>\n                    </div>\n\n                    {/* Click indicator */}\n                    <div className=\"text-gray-400 group-hover:text-[#007AFF] transition-colors\">\n                      <svg\n                        className=\"w-6 h-6\"\n                        fill=\"none\"\n                        stroke=\"currentColor\"\n                        viewBox=\"0 0 24 24\"\n                      >\n                        <path\n                          strokeLinecap=\"round\"\n                          strokeLinejoin=\"round\"\n                          strokeWidth={2}\n                          d=\"M15 19l-7-7 7-7\"\n                        />\n                      </svg>\n                    </div>\n                  </div>\n                </div>\n              ))}\n            </div>\n          </div>\n        );\n      })}\n\n      {/* Appointment Modal */}\n      {selectedDate && (\n        <AppointmentModal\n          date={selectedDate.date}\n          appointments={selectedDate.appointments}\n          holidays={selectedDate.holidays}\n          shabbat={selectedDate.shabbat}\n          onClose={handleCloseModal}\n          onUpdate={onUpdate}\n        />\n      )}\n    </div>\n  );\n}\n\nexport default ListView;"
    },
    "components/AppointmentModal.js": {
      "description": "מודאל לתצוגת/עריכת/יצירת תורים. כולל הצגת חגים ושבת, טופס יצירה, עריכה ומחיקה.",
      "exports": ["AppointmentModal (default)"],
      "dependencies": ["../services/api (createAppointment, updateAppointment, deleteAppointment)"],
      "code": "import { useState } from 'react';\nimport { createAppointment, updateAppointment, deleteAppointment } from '../services/api';\n\nfunction AppointmentModal({ date, appointments, holidays, shabbat, onClose, onUpdate }) {\n  const [editingId, setEditingId] = useState(null);\n  const [editForm, setEditForm] = useState({});\n  const [showCreateForm, setShowCreateForm] = useState(false);\n  const [createForm, setCreateForm] = useState({\n    name: '',\n    address: '',\n    phone: '',\n    timeRange: '',\n    jobNature: '',\n    date: date\n  });\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState(null);\n\n  // Format date for display\n  const formattedDate = new Date(date).toLocaleDateString('he-IL', {\n    weekday: 'long',\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric'\n  });\n\n  // --- Create ---\n  const handleShowCreate = () => {\n    setShowCreateForm(true);\n    setEditingId(null);\n    setError(null);\n  };\n\n  const handleCancelCreate = () => {\n    setShowCreateForm(false);\n    setCreateForm({\n      name: '',\n      address: '',\n      phone: '',\n      timeRange: '',\n      jobNature: '',\n      date: date\n    });\n    setError(null);\n  };\n\n  const handleCreate = async () => {\n    if (!createForm.name.trim() || !createForm.address.trim()) {\n      setError('שם וכתובת הם שדות חובה');\n      return;\n    }\n\n    setLoading(true);\n    setError(null);\n\n    try {\n      await createAppointment(createForm);\n      setShowCreateForm(false);\n      setCreateForm({\n        name: '',\n        address: '',\n        phone: '',\n        timeRange: '',\n        jobNature: '',\n        date: date\n      });\n      onUpdate();\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  // --- Edit ---\n  const handleEdit = (appointment) => {\n    setEditingId(appointment._id);\n    setShowCreateForm(false);\n    setEditForm({\n      name: appointment.name,\n      address: appointment.address,\n      phone: appointment.phone || '',\n      timeRange: appointment.timeRange || '',\n      jobNature: appointment.jobNature || '',\n      date: appointment.date.split('T')[0]\n    });\n    setError(null);\n  };\n\n  const handleCancelEdit = () => {\n    setEditingId(null);\n    setEditForm({});\n    setError(null);\n  };\n\n  const handleSaveEdit = async (appointmentId) => {\n    setLoading(true);\n    setError(null);\n\n    try {\n      await updateAppointment(appointmentId, editForm);\n      setEditingId(null);\n      setEditForm({});\n      onUpdate();\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  // --- Delete ---\n  const handleDelete = async (appointmentId, appointmentName) => {\n    if (!window.confirm(`העבר את התור של ${appointmentName} לסל מחזור?`)) {\n      return;\n    }\n\n    setLoading(true);\n    setError(null);\n\n    try {\n      await deleteAppointment(appointmentId);\n      onUpdate();\n\n      if (appointments.length === 1) {\n        onClose();\n      }\n    } catch (err) {\n      setError(err.message);\n    } finally {\n      setLoading(false);\n    }\n  };\n\n  const inputClass = \"w-full px-4 py-3 border border-gray-300 rounded-xl focus:ring-2 focus:ring-[#007AFF] focus:border-transparent\";\n\n  return (\n    <div\n      className=\"fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-end sm:items-center justify-center p-0 sm:p-4\"\n      onClick={onClose}\n    >\n      <div\n        className=\"bg-white rounded-t-3xl sm:rounded-3xl w-full sm:max-w-2xl max-h-[90vh] overflow-hidden shadow-2xl animate-slide-up\"\n        onClick={(e) => e.stopPropagation()}\n      >\n        {/* Header */}\n        <div className=\"sticky top-0 bg-white border-b border-gray-200 p-6 flex justify-between items-start\">\n          <div>\n            <h2 className=\"text-2xl font-bold text-gray-900\">{formattedDate}</h2>\n            <p className=\"text-sm text-gray-500 mt-1\">\n              {appointments.length > 0\n                ? `${appointments.length} תור${appointments.length > 1 ? 'ים' : ''}`\n                : 'אין תורים'\n              }\n            </p>\n          </div>\n          <button\n            onClick={onClose}\n            className=\"text-gray-400 hover:text-gray-600 text-3xl leading-none transition-colors\"\n          >\n            ×\n          </button>\n        </div>\n\n        {/* Content */}\n        <div className=\"overflow-y-auto max-h-[calc(90vh-100px)] p-6 space-y-4\">\n          {/* Error Message */}\n          {error && (\n            <div className=\"bg-red-50 border border-red-200 rounded-xl p-4 text-red-700\">\n              {error}\n            </div>\n          )}\n\n          {/* Holidays & Shabbat */}\n          {(holidays.length > 0 || shabbat) && (\n            <div className=\"bg-gradient-to-br from-orange-50 to-amber-50 rounded-2xl p-4 space-y-2\">\n              {holidays.map((holiday, idx) => (\n                <div key={idx} className=\"flex items-center gap-2\">\n                  <span className=\"text-2xl\">🕎</span>\n                  <span className=\"text-orange-900 font-semibold\">{holiday.text}</span>\n                </div>\n              ))}\n              {shabbat && (\n                <div className=\"flex items-center gap-2\">\n                  <span className=\"text-2xl\">🕯️</span>\n                  <span className=\"text-gray-700\">{shabbat.text}</span>\n                  {shabbat.time && (\n                    <span className=\"text-gray-500 text-sm\">({shabbat.time})</span>\n                  )}\n                </div>\n              )}\n            </div>\n          )}\n\n          {/* Add New Appointment Button */}\n          {!showCreateForm && (\n            <button\n              onClick={handleShowCreate}\n              className=\"w-full bg-green-500 text-white px-4 py-3 rounded-xl font-medium hover:bg-green-600 active:scale-[0.98] transition-all text-sm\"\n            >\n              + תור חדש\n            </button>\n          )}\n\n          {/* Create Form */}\n          {showCreateForm && (\n            <div className=\"bg-green-50 border-2 border-green-300 rounded-2xl p-4 space-y-3\">\n              <h3 className=\"font-bold text-green-800 text-lg\">תור חדש</h3>\n              <input\n                type=\"text\"\n                value={createForm.name}\n                onChange={(e) => setCreateForm({ ...createForm, name: e.target.value })}\n                placeholder=\"שם *\"\n                className={inputClass}\n              />\n              <input\n                type=\"text\"\n                value={createForm.address}\n                onChange={(e) => setCreateForm({ ...createForm, address: e.target.value })}\n                placeholder=\"כתובת *\"\n                className={inputClass}\n              />\n              <input\n                type=\"tel\"\n                value={createForm.phone}\n                onChange={(e) => setCreateForm({ ...createForm, phone: e.target.value })}\n                placeholder=\"טלפון\"\n                className={inputClass}\n              />\n              <input\n                type=\"text\"\n                value={createForm.timeRange}\n                onChange={(e) => setCreateForm({ ...createForm, timeRange: e.target.value })}\n                placeholder=\"שעות (למשל: 10:00-12:00)\"\n                className={inputClass}\n              />\n              <textarea\n                value={createForm.jobNature}\n                onChange={(e) => setCreateForm({ ...createForm, jobNature: e.target.value })}\n                placeholder=\"מהות העבודה\"\n                rows=\"3\"\n                className={`${inputClass} resize-none`}\n              />\n\n              <div className=\"flex gap-2 pt-2\">\n                <button\n                  onClick={handleCreate}\n                  disabled={loading}\n                  className=\"flex-1 bg-green-500 text-white px-4 py-3 rounded-xl font-medium hover:bg-green-600 active:scale-[0.98] transition-all disabled:opacity-50\"\n                >\n                  {loading ? 'שומר...' : 'שמור תור'}\n                </button>\n                <button\n                  onClick={handleCancelCreate}\n                  disabled={loading}\n                  className=\"flex-1 bg-gray-100 text-gray-700 px-4 py-3 rounded-xl font-medium hover:bg-gray-200 active:scale-[0.98] transition-all disabled:opacity-50\"\n                >\n                  ביטול\n                </button>\n              </div>\n            </div>\n          )}\n\n          {/* Existing Appointments */}\n          <div className=\"space-y-3\">\n            {appointments.map((appointment) => (\n              <div\n                key={appointment._id}\n                className=\"bg-white border-2 border-gray-200 rounded-2xl overflow-hidden hover:border-[#007AFF] transition-all\"\n              >\n                {editingId === appointment._id ? (\n                  /* Edit Mode */\n                  <div className=\"p-4 space-y-3\">\n                    <input\n                      type=\"text\"\n                      value={editForm.name}\n                      onChange={(e) => setEditForm({ ...editForm, name: e.target.value })}\n                      placeholder=\"שם\"\n                      className={inputClass}\n                    />\n                    <input\n                      type=\"text\"\n                      value={editForm.address}\n                      onChange={(e) => setEditForm({ ...editForm, address: e.target.value })}\n                      placeholder=\"כתובת\"\n                      className={inputClass}\n                    />\n                    <input\n                      type=\"tel\"\n                      value={editForm.phone}\n                      onChange={(e) => setEditForm({ ...editForm, phone: e.target.value })}\n                      placeholder=\"טלפון\"\n                      className={inputClass}\n                    />\n                    <input\n                      type=\"text\"\n                      value={editForm.timeRange}\n                      onChange={(e) => setEditForm({ ...editForm, timeRange: e.target.value })}\n                      placeholder=\"שעות (למשל: 10:00-12:00)\"\n                      className={inputClass}\n                    />\n                    <textarea\n                      value={editForm.jobNature}\n                      onChange={(e) => setEditForm({ ...editForm, jobNature: e.target.value })}\n                      placeholder=\"תיאור העבודה\"\n                      rows=\"3\"\n                      className={`${inputClass} resize-none`}\n                    />\n\n                    <div className=\"flex gap-2 pt-2\">\n                      <button\n                        onClick={() => handleSaveEdit(appointment._id)}\n                        disabled={loading}\n                        className=\"flex-1 bg-[#007AFF] text-white px-4 py-3 rounded-xl font-medium hover:bg-[#0056CC] active:scale-[0.98] transition-all disabled:opacity-50\"\n                      >\n                        {loading ? 'שומר...' : 'שמור'}\n                      </button>\n                      <button\n                        onClick={handleCancelEdit}\n                        disabled={loading}\n                        className=\"flex-1 bg-gray-100 text-gray-700 px-4 py-3 rounded-xl font-medium hover:bg-gray-200 active:scale-[0.98] transition-all disabled:opacity-50\"\n                      >\n                        ביטול\n                      </button>\n                    </div>\n                  </div>\n                ) : (\n                  /* View Mode */\n                  <div className=\"p-4\">\n                    <div className=\"space-y-3\">\n                      {/* Name */}\n                      <div className=\"flex items-start gap-3\">\n                        <span className=\"text-2xl\">👤</span>\n                        <div className=\"flex-1\">\n                          <div className=\"text-xs text-gray-500 mb-0.5\">שם</div>\n                          <div className=\"text-lg font-semibold text-gray-900\">\n                            {appointment.name}\n                          </div>\n                        </div>\n                      </div>\n\n                      {/* Address */}\n                      <div className=\"flex items-start gap-3\">\n                        <span className=\"text-2xl\">📍</span>\n                        <div className=\"flex-1\">\n                          <div className=\"text-xs text-gray-500 mb-0.5\">כתובת</div>\n                          <div className=\"text-gray-700\">{appointment.address}</div>\n                        </div>\n                      </div>\n\n                      {/* Phone */}\n                      {appointment.phone && (\n                        <div className=\"flex items-start gap-3\">\n                          <span className=\"text-2xl\">📞</span>\n                          <div className=\"flex-1\">\n                            <div className=\"text-xs text-gray-500 mb-0.5\">טלפון</div>\n                            <a\n                              href={`tel:${appointment.phone}`}\n                              className=\"text-[#007AFF] hover:underline\"\n                            >\n                              {appointment.phone}\n                            </a>\n                          </div>\n                        </div>\n                      )}\n\n                      {/* WhatsApp & Waze */}\n                      <div className=\"flex gap-2 flex-wrap\">\n                        {appointment.phone && (\n                          <a\n                            href={`https://wa.me/${appointment.phone.replace(/^0/, '972')}`}\n                            target=\"_blank\"\n                            rel=\"noopener noreferrer\"\n                            className=\"inline-flex items-center gap-1.5 bg-green-500 text-white px-4 py-2 rounded-xl text-sm font-medium hover:bg-green-600 active:scale-[0.98] transition-all\"\n                          >\n                            💬 WhatsApp\n                          </a>\n                        )}\n                        {appointment.address && (\n                          <a\n                            href={`https://waze.com/ul?q=${encodeURIComponent(appointment.address)}`}\n                            target=\"_blank\"\n                            rel=\"noopener noreferrer\"\n                            className=\"inline-flex items-center gap-1.5 bg-[#33ccff] text-white px-4 py-2 rounded-xl text-sm font-medium hover:bg-[#00b8f0] active:scale-[0.98] transition-all\"\n                          >\n                            🗺️ Waze\n                          </a>\n                        )}\n                      </div>\n\n                      {/* Time Range */}\n                      {appointment.timeRange && (\n                        <div className=\"flex items-start gap-3\">\n                          <span className=\"text-2xl\">🕐</span>\n                          <div className=\"flex-1\">\n                            <div className=\"text-xs text-gray-500 mb-0.5\">שעות</div>\n                            <div className=\"text-gray-700 font-medium\">\n                              {appointment.timeRange}\n                            </div>\n                          </div>\n                        </div>\n                      )}\n\n                      {/* Job Nature */}\n                      {appointment.jobNature && (\n                        <div className=\"flex items-start gap-3\">\n                          <span className=\"text-2xl\">📝</span>\n                          <div className=\"flex-1\">\n                            <div className=\"text-xs text-gray-500 mb-0.5\">תיאור</div>\n                            <div className=\"text-gray-700 whitespace-pre-wrap\">\n                              {appointment.jobNature}\n                            </div>\n                          </div>\n                        </div>\n                      )}\n                    </div>\n\n                    {/* Action Buttons */}\n                    <div className=\"flex gap-2 mt-4 pt-4 border-t border-gray-200\">\n                      <button\n                        onClick={() => handleEdit(appointment)}\n                        disabled={loading}\n                        className=\"flex-1 bg-[#007AFF] text-white px-4 py-2.5 rounded-xl font-medium hover:bg-[#0056CC] active:scale-[0.98] transition-all disabled:opacity-50 text-sm\"\n                      >\n                        ✏️ ערוך\n                      </button>\n                      <button\n                        onClick={() => handleDelete(appointment._id, appointment.name)}\n                        disabled={loading}\n                        className=\"flex-1 bg-red-50 text-red-600 px-4 py-2.5 rounded-xl font-medium hover:bg-red-100 active:scale-[0.98] transition-all disabled:opacity-50 text-sm\"\n                      >\n                        🗑️ סל מחזור\n                      </button>\n                    </div>\n                  </div>\n                )}\n              </div>\n            ))}\n          </div>\n        </div>\n      </div>\n\n      <style jsx>{`\n        @keyframes slide-up {\n          from {\n            transform: translateY(100%);\n          }\n          to {\n            transform: translateY(0);\n          }\n        }\n        .animate-slide-up {\n          animation: slide-up 0.3s ease-out;\n        }\n      `}</style>\n    </div>\n  );\n}\n\nexport default AppointmentModal;"
    },
    "styles/index.css": {
      "description": "סגנונות בסיס - פונטים ו-reset",
      "code": "body {\n  margin: 0;\n  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen',\n    'Ubuntu', 'Cantarell', 'Fira Sans', 'Droid Sans', 'Helvetica Neue',\n    sans-serif;\n  -webkit-font-smoothing: antialiased;\n  -moz-osx-font-smoothing: grayscale;\n}\n\ncode {\n  font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New',\n    monospace;\n}"
    }
  },
  "integrationGuide": {
    "description": "הוראות לסוכן כדי לשלב את לוח השנה בפרויקט אחר",
    "steps": [
      {
        "step": 1,
        "title": "התקנת תלויות",
        "description": "הפרויקט משתמש ב-React ו-Tailwind CSS. ודא שהם מותקנים.",
        "commands": ["npm install react react-dom react-router-dom", "npm install tailwindcss @tailwindcss/forms"]
      },
      {
        "step": 2,
        "title": "יצירת קבצי השירותים",
        "description": "צור את services/hebcal.js כמו שהוא - זה ה-API לנתונים העבריים. התאם את services/api.js ל-endpoints של הפרויקט החדש."
      },
      {
        "step": 3,
        "title": "יצירת ה-hook",
        "description": "צור את hooks/useCalendarData.js. אם לפרויקט החדש יש מבנה data אחר, התאם את הפונקציה fetchAppointments."
      },
      {
        "step": 4,
        "title": "יצירת הקומפוננטות",
        "description": "צור את 3 הקומפוננטות: CalendarView.js, ListView.js, AppointmentModal.js. התאם את ה-imports לפי מבנה הפרויקט."
      },
      {
        "step": 5,
        "title": "התאמות נדרשות",
        "items": [
          "שנה את ה-imports של auth (isAuthenticated, isAdmin) למנגנון ה-auth של הפרויקט החדש",
          "שנה את ה-API endpoints ב-api.js",
          "אם רוצים מיקום אחר (לא תל אביב), שנה את geonameid ב-hebcal.js",
          "אם רוצים לוח שנה בלבד (בלי תורים), אפשר להסיר את כל מה שקשור ל-appointments ולהשאיר רק את ה-Hebcal"
        ]
      }
    ],
    "calendarOnlyMode": {
      "description": "אם רוצים רק את לוח השנה העברי בלי מערכת תורים, צריך:",
      "changes": [
        "ב-useCalendarData.js - להסיר את fetchAppointments ולהשאיר רק את fetchHebcalData",
        "ב-CalendarView.js - להסיר את כל מה שקשור ל-appointments, להשאיר רק holidays/shabbat/parasha",
        "להסיר את AppointmentModal.js",
        "להסיר את services/api.js"
      ]
    }
  },
  "hebcalAPIReference": {
    "baseURL": "https://www.hebcal.com/hebcal",
    "params": {
      "v": "1 (API version)",
      "cfg": "json (response format)",
      "maj": "on (major holidays)",
      "min": "on (minor holidays)",
      "mod": "on (modern holidays)",
      "nx": "on (Rosh Chodesh)",
      "s": "on (Shabbat times)",
      "i": "on (Israeli holidays, not diaspora)",
      "lg": "h (Hebrew language)",
      "geo": "geoname (geolocation method)",
      "geonameid": "281184 (Tel Aviv - change for different city)",
      "year": "2026 (year to fetch)",
      "month": "2 (month to fetch, 1-indexed)"
    },
    "responseStructure": {
      "items": [
        {
          "title": "English event name",
          "hebrew": "שם האירוע בעברית (USE THIS FOR DISPLAY)",
          "date": "2026-02-14",
          "category": "holiday | parashat | candles | havdalah"
        }
      ]
    },
    "commonGeonameIds": {
      "תל אביב": 281184,
      "ירושלים": 281184,
      "חיפה": 294801,
      "באר שבע": 295530,
      "ניו יורק": 5128581,
      "לוס אנג'לס": 5368361,
      "לונדון": 2643743
    }
  }
}